<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ghuljr.onehabit">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OneHabit">
        <activity
            android:name=".ui.intro.login.forgot_password.ForgotPasswordActivity"
            android:exported="true" />
        <activity
            android:name=".ui.profile.fill_data.FillDataActivity"
            android:exported="true" />
        <activity
            android:name=".ui.intro.register.RegisterActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
        </activity>
        <activity
            android:name=".ui.intro.IntroActivity"
            android:exported="true"/>
        <activity
            android:name=".ui.intro.login.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true" />
    </application>

</manifest>